import React from 'react';
import { Link } from 'react-router-dom';

const SaveButton = () => {
  return (
    <button type="submit" className="btn btn-outline-dark">Save and Continue</button>
  );
};

export default SaveButton;
